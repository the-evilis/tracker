# Локальные настройки со строкой подключения к базе и токенами.
# В репозиторий не попадают — шаблон лежит в .env.example.
.env
bot/.env

# Архивы бэкапов
backups/

# Зависимости и артефакты тестов
node_modules/
test-results/
playwright-report/
blob-report/
.playwright-mcp/

# Личные разрешения Claude Code для этой машины (доступ к серверу, git, npm).
.claude/settings.local.json
